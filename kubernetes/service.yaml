apiVersion: v1
kind: Service
metadata:
  name: federated-claims-analyzer
  labels:
    app: federated-claims-analyzer
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: federated-claims-analyzer
